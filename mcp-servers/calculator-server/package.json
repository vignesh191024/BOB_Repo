{
  "name": "calculator-server",
  "version": "0.1.0",
  "description": "A simple calculator MCP server for Bob Sandbox testing",
  "type": "module",
  "bin": {
    "calculator-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "keywords": [
    "mcp",
    "calculator",
    "bob-sandbox"
  ],
  "author": "Bob Sandbox",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}